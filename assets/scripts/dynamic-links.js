let indexLinks = Array.from(document.querySelectorAll('a'));
const trimIndex = (str) => {
  if (str.slice(-5 === 'index')) {
    // Replace that final 'index' with an empty string -- but only the final 'index', in case there are multiples in the pathname on the way there.
  }
}